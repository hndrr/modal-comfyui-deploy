[project]
name = "modal-comfyui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=5.47.2",
    "modal>=1.1.4",
    "pillow>=11.0.0",
    "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
ambient-test = ["fastapi>=0.115.14", "httpx>=0.28.1", "aiohttp>=3.12.15", "python-multipart>=0.0.22"]

[tool.flake8]
max-line-length = 100
